NETWORK=<Network>
CORE_URL=<CoreUrl>

# Server. Core already listens on 3000, so the demo takes 3100.
PORT=<Port>
PAY_TO_ADDRESS=<PayToAddress>
FACILITATOR_URL=<FacilitatorUrl>

# Client and deposit. Anvil #1: holds mock USDC and ETH.
PRIVATE_KEY=<PrivateKey>
API_URL=<ApiUrl>
DEPOSIT_AMOUNT=<DepositAmount>
