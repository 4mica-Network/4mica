{
  "name": "facilitator-4mica",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/4mica-Network/facilitator-4mica.git"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "fmt": "pnpm -r run fmt",
    "clean": "pnpm -r exec rm -rf dist && rm -rf node_modules",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "catalog:"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@10.28.2",
  "publishConfig": {
    "access": "public"
  }
}
