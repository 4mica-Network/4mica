{
  "name": "@4mica/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Clerk session authentication plugin for Fastify.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@clerk/backend": "catalog:",
    "fastify-plugin": "catalog:"
  },
  "peerDependencies": {
    "fastify": ">=5"
  },
  "devDependencies": {
    "@4mica/tsconfig": "workspace:*",
    "@types/node": "catalog:node24",
    "fastify": "catalog:",
    "typescript": "catalog:typescript-latest",
    "vitest": "catalog:"
  },
  "publishConfig": {
    "access": "public"
  }
}
